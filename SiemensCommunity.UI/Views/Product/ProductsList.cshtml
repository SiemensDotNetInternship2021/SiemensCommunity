@model IEnumerable<SiemensCommunity.Domain.Models.Entities.Product>

<h1>Books List</h1>
@foreach (var product in Model)
            {
                <div>
                    <h3>@product.Name</h3>
                </div>
            }